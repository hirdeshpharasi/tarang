
4.0.1
29/09/2009

Precision included in the revision.

Changes in 
* extern_def_vars.h: 
along with #ifndef DP
const int MY_PRECISION = 8;

* basis_basicfn_inline.h
#include <iomanip>

* file_operaions.cc
global_file.setf(ios::fixed, ios::floatfield);
global_file.precision(MY_PRECISION); // sets number of decimal places

****************************************
3/10/2009.

In output_field.cc
for functions Output_field_r(......)
replaced 
field_r_buf(1) =  real((*V1r)(kx,ky,kz)); 
by
field_r_buf(1) =  real((*V1r)(lx,ly,lz)); 
